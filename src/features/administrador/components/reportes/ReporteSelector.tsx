import { Pressable, Text, View } from "react-native";
export function ReporteSelector({ reportes, onSelect }: { reportes: ReadonlyArray<{ id: string; label: string; icon: string }>; onSelect: (id: string) => void }) { return <>{reportes.map((report) => <Pressable key={report.id} onPress={() => onSelect(report.id)} className="flex-row items-center gap-3 rounded-2xl bg-white p-5" style={{ elevation: 2, shadowColor: "#000", shadowOpacity: 0.06, shadowRadius: 7, shadowOffset: { width: 0, height: 2 } }}><View className="h-12 w-12 shrink-0 items-center justify-center rounded-full bg-gray-100"><Text className="text-2xl">{report.icon}</Text></View><Text className="flex-1 text-base font-semibold text-gray-900">{report.label}</Text></Pressable>)}</>; }
