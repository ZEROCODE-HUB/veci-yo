import { Ionicons } from "@expo/vector-icons";
import { Pressable, Text, View } from "react-native";
import type { DirectorioEstacionamiento } from "../types/directorio";
export function DirectorioEstacionamientoCard({ item, onPress }: { item: DirectorioEstacionamiento; onPress: () => void }) { return <Pressable onPress={onPress} className="active:opacity-80" style={{ backgroundColor: "#fff", borderRadius: 20, paddingVertical: 14, paddingHorizontal: 16, boxShadow: "0 2px 8px rgba(0,0,0,0.08)" }}><View style={{ flexDirection: "row", justifyContent: "space-between", alignItems: "center" }}><View style={{ flex: 1 }}><Text className="text-sm font-bold text-gray-900">Estacionamiento {item.codigo} — Torre {item.torreNumero}</Text><Text className="text-xs text-gray-500">Ubicación: {item.ubicacion} → Depto {item.unidad.codigo}</Text><Text className="text-xs text-gray-500">Propietario: {item.contactos.propietario.nombre}</Text></View><Ionicons name="chevron-forward" size={18} color="#9CA3AF" /></View><Text className="text-xs text-gray-500" style={{ marginTop: 8 }}>Propietario: {item.contactos.propietario.nombre} · Anfitrión primario: {item.contactos.anfitrion.nombre} · Admin: {item.contactos.administrador.nombre}</Text></Pressable>; }
