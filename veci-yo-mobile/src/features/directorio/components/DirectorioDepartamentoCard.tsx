import { Ionicons } from "@expo/vector-icons";
import { Pressable, Text, View } from "react-native";
import type { Tipologia, Unidad } from "../types/directorio";
import type { DirectorioContactos } from "../types/directorio";
export function DirectorioDepartamentoCard({ item, tipologias, contactos, anfitrionPrimario, onPress }: { item: Unidad; tipologias: Tipologia[]; contactos: DirectorioContactos; anfitrionPrimario?: string; onPress: () => void }) { const tipologia = tipologias.find((value) => value.id === item.tipologiaId); const tipologiaNombre = tipologia?.nombre === "Estandar" ? "Estándar" : tipologia?.nombre; const esPrimario = Boolean(item.propietarioAsignado && anfitrionPrimario && item.propietarioAsignado === anfitrionPrimario); return <Pressable onPress={onPress} className="active:opacity-80" style={{ backgroundColor: "#fff", borderRadius: 20, paddingVertical: 14, paddingHorizontal: 16, boxShadow: "0 2px 8px rgba(0,0,0,0.08)" }}><View style={{ flexDirection: "row", justifyContent: "space-between", alignItems: "center" }}><View style={{ flex: 1 }}><Text className="text-sm font-bold text-gray-900">Torre {item.torreNumero} → Depto {item.codigo} {tipologiaNombre ? `(${tipologiaNombre})` : ""}</Text><Text className="text-xs text-gray-500">Piso {item.piso} · {item.estado}</Text><View className="flex-row flex-wrap items-center"><Text className="text-xs text-gray-500">Propietario: {item.propietarioAsignado || "—"}</Text>{esPrimario ? <View className="ml-1 rounded-full bg-primary-light px-1.5 py-0.5"><Text className="text-[10px] font-bold text-primary">Primario</Text></View> : null}</View></View><Ionicons name="chevron-forward" size={18} color="#9CA3AF" /></View><View style={{ marginTop: 8, flexDirection: "row", flexWrap: "wrap", gap: 6 }}><View className="rounded-full" style={{ backgroundColor: "#F3F4F6", paddingHorizontal: 8, paddingVertical: 2 }}><Text className="text-gray-500" style={{ fontSize: 10 }}>🏠 Estac: {item.estacionamientos ?? 0}</Text></View><View className="rounded-full" style={{ backgroundColor: "#F3F4F6", paddingHorizontal: 8, paddingVertical: 2 }}><Text className="text-gray-500" style={{ fontSize: 10 }}>📦 Depósitos asociados al depto</Text></View></View><Text className="text-xs text-gray-500" style={{ marginTop: 8 }}>Propietario: {contactos.propietario.nombre} · Anfitrión primario: {contactos.anfitrion.nombre} · Admin: {contactos.administrador.nombre}</Text></Pressable>; }
